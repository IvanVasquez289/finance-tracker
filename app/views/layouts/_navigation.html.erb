<nav class="navbar navbar-expand-lg navbar navbar-dark" style="background-color: #000000;">
    <div class="container">
        <%# <a class="navbar-brand" href="#">Navbar</a> %>
        <%= link_to 'FinanceTracker', root_path, class:'navbar-brand' %>
        <% if current_user %>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <% if request.path == my_portfolio_path %>
                            <%= link_to 'Mi portafolio', my_portfolio_path, class:'nav-link active' %>
                        <% else %>
                            <%= link_to 'Mi portafolio', my_portfolio_path, class:'nav-link' %>
                        <% end %>   
                    </li>
                    <li class="nav-item">
                        <% if request.path == my_friends_path %>
                            <%= link_to 'Amigos', my_friends_path, class:'nav-link active' %>
                        <% else %>
                            <%= link_to 'Amigos', my_friends_path, class:'nav-link' %>
                        <% end %>  
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Perfil
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <%= link_to edit_user_registration_path, class:'dropdown-item' do%>
                                <i class="bi bi-pencil-square"></i>
                                Editar perfil
                            <% end %>   
                            <li><a class="dropdown-item" href="#">Another action</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="#">Something else here</a></li>
                        </ul>
                    </li>
                </ul>

                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                    <li class='nav-item nombre-usuario'>
                        <i class="bi bi-person-circle"></i>
                        <%= current_user.full_name%>
                    </li>
                    <li class='nav-item'>
                        <%= link_to 'Cerrar Sesion', destroy_user_session_path, class:'btn btn-danger logout', method: :delete %>
                    </li>
                </ul>
                <%# <form class="d-flex">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
                </form> %>
            </div>
        <% end %>
    </div>
</nav>